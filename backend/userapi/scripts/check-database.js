#!/usr/bin/env node
/* Kiểm tra cơ bản dữ liệu Mongo (Users, Bookings) bằng models đã hợp nhất. */
require('dotenv').config();

throw new Error('Deprecated Mongo script removed. Use PostgreSQL queries instead.');
